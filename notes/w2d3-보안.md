# 보안

## 비밀번호 해싱

- 입력값을 넣으면 항상 같은 결과가 나오지만, 원래 입력값을 알 수 없는 방식으로 변환하는 것
- 해싱된 비밀번호는 원래 비밀번호로 되돌릴 수 없습니다.
- 해싱된 비밀번호를 저장하면, 데이터베이스가 유출되더라도 원래 비밀번호가 노출되지 않습니다.
- 일반적으로 bcrypt, Argon2, PBKDF 같은 강력한 해싱 알고리즘을 사용합니다.
